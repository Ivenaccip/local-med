"Cargar un modelo de m s de 100 MB necesita un git LFS, lo mejor es pasarlo en el gitignore para el github y descargarlo de forma local" 
"Tenemos que usar el mismo modelo para el RAG como para los embedding que se procesen"  
"flutter_embedder tiene hardocdeado un modelo por default all-MiniLM-L6-v2 el cual solo esta disponbible en ingl‚s, por eso vamos a usar el modelo embeddingGemma 300m"  
